// Çıkış düğmesini seçme
const exitButton = document.getElementById('exit-button');

// Çıkış düğmesine tıklandığında
exitButton.addEventListener('click', function() {
    // Sekmenin kapatılması için "window.close()" kullanılır
    window.close();
});
